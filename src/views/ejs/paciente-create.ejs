<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Crear nuevo paciente</title> 
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

    <header>
        <h2>Clínica Salud+</h2>
        <nav>
          <a href="/pacientes">Listado de pacientes</a>                   
          <a href="/pacientes/create">Crear nuevo paciente</a>
          <a href="/pacientes/search">Buscar / Borrar Paciente</a>
          <a href="/pacientes/logout">Cerrar sesión</a>  
        </nav>
    </header>

    <main>

      <% if (error && error.length > 0) { %>
        <div class="flash error"><%= error[0] %></div>
      <% } %>
      <% if (success && success.length > 0) { %>
        <div class="flash success"><%= success[0] %></div>
      <% } %>


      <h2 style="text-align:center;">Agregar un nuevo paciente</h2>   

      <form action="/pacientes/create" method="POST">
        <div class="form-group">
          <label for="nombre">Nombre:</label>
          <input
            type="text"
            id="name"
            name="name"
            required
            value="<%= paciente.name || '' %>"
          />
        </div>

        <div class="form-group">
          <label for="email">e-mail:</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            value="<%= paciente.email || '' %>"
          />
        </div>

        <div class="form-group">
          <label for="password">Contraseña:</label>
          <input
            type="password"
            id="password"
            name="password"
            required
          />
        </div>
        <div class="form-button"><button type="submit">Crear Paciente</button></div>
      </form>

    </main>

    <footer>
            <p>© <%= new Date().getFullYear() %> Clínica Salud+ - Todos los derechos reservados.</p>
    </footer>

</body>

</html>